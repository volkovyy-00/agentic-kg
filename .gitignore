# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.ruff_cache/
.scannerwork/
# Coverage output (CI generates coverage.xml for SonarCloud)
.coverage
coverage.xml

# Virtual environments
.venv
.env

# ADK web's per-agent session store (google-adk >=1.28 writes .adk/session.db next to each agent)
.adk/

# IDEs
.vscode
.idea
.cursorrules
.ropeproject

# Local scratch content, not part of the project
prototype/

# Superpowers / SDD scratch workspace (process artifacts, not deliverables)
.superpowers/

# Design docs and plans -- kept locally, not published
docs/superpowers/

# Local defect/follow-up notes, not a committed project artifact
docs/backlog/
